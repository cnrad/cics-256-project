#include "src/matrix/matrix.h"
#include "src/cursor/cursor.h"

class Game
{
public:
    void start() {
    };

    void update() {
        // for (every gameobject) {
        //     gameobject.onLoop(); // onloop just checks cursor for cover or click state and executes accordingly
        // }

    };
};